# ============================================================================
# 模型推理服务专用依赖
# ============================================================================

# PyTorch (CPU 版本，生产环境可换 CUDA 版本)
torch==2.1.0
--index-url https://download.pytorch.org/whl/cpu

# Scikit-learn (scalers)
scikit-learn==1.3.2
